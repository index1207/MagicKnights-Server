import {Session} from "./session";

export function OnRecvPacket(session: Session, buffer: Buffer) {
    /*const pktId: number = buffer.readUint16BE(0);
    const data = buffer.subarray(2, 2 - buffer.length);*/
}